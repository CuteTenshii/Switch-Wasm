[package]
name = "switch-gpu"
version = "0.1.0"
edition = "2021"
description = "A GPU backend for switch-core's 3D engine, on wgpu"
license = "MIT"

[dependencies]
pollster = "1.0.1"
switch-core = { path = "../switch-core" }
web-time = "1.1"
wgpu = "30.0.1"
