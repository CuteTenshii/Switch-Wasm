/target
/node_modules
# The built site: `make assets` generates every file in it.
/dist
*.wasm
*.nro
.DS_Store
*.bndb
*.keys
*.nsp
*.nca
*.xci

# Decrypted game binaries and symbol maps (dump_exefs output).
out/

# Decrypted game binaries staged for static analysis. The READMEs beside them
# are the analysis and stay tracked; the images are 165 MB and are reproducible
# from the container with `dump_exefs`.
/home_menu_bn/*.bin
/home_menu_bn/raw_*
/tomodachi_bn/*.bin
