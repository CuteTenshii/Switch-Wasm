{
  "name": "switch-wasm-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.worker.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.2.2"
  }
}
